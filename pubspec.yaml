name: atem
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'  # DartのバージョンをFlutter 2の推奨バージョンに変更

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.20.0
  flutter_riverpod: ^2.4.4
  cupertino_icons: ^1.0.6
  firebase_auth: ^4.12.0
  http: ~1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
